{% extends 'base.html.twig' %}

{% block title %}Les Sous Familles D'instruments{% endblock %}

{% block body %}
    <h1>Les Sous Familles D'instruments</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Libelle</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for subfamilly in subfamillies %}
            <tr>
                <td>{{ subfamilly.id }}</td>
                <td>{{ subfamilly.libelle }}</td>
                <td>
                    <a href="{{ path('app_subfamilly_show', {'id': subfamilly.id}) }}">show</a>
                    <a href="{{ path('app_subfamilly_edit', {'id': subfamilly.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_subfamilly_new') }}">Create new</a>
{% endblock %}
